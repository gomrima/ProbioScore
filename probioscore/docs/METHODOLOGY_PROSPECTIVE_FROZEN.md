# Methodology prospective frozen

Prospective frozen mode uses fixed thresholds and embedded curated governance tables. It does not read external labels and does not recalibrate on the evaluated panel.

This is the only appropriate mode for future unseen V4 outputs. It writes `Ounissi_used_for_calibration=False` and records the input panel name when provided.
